# news-dedup-engine
Real-time near-duplicate news detection pipeline with tiered latency architecture. Classifies semantically equivalent news from different sources (e.g. Reuters vs PR Newswire) within a configurable time window. Supports fast approximate matching (LSH/MinHash) and high-accuracy semantic reranking
